[project]
name = "mitodev"
version = "0.1.0"
description = "Mito development environment"
requires-python = ">=3.10,<3.11"

dependencies = [
    "numpy",
    "scikit-image",
    "pyyaml",
    "scikit-learn",
    "matplotlib",
    "pandas",
]